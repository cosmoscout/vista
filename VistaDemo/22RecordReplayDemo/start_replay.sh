#!/bin/bash

# In this file, we run the demo RecordReplayDemo with -replay, which replays a prevously recorded session

source ./set_path_for_22RecordReplayDemo.sh

./22RecordReplayDemo -replay testrecord
